- var regFormCss = '/css/components/forms/registrationForm.css'

html
    head
        link(rel="stylesheet" href=regFormCss)
    body
        block layout-content
        form(id="user-form" class="materialize glowing-edge")

            label(for="firstName") First Name:
            input(type="text" id="firstName" name="firstName" required)

            label(for="lastName") Last Name:
            input(type="text" id="lastName" name="lastName" required)

            label(for="username") Username:
            input(type="text" id="username" name="username" required)

            label(for="email") Email:
            input(type="email" id="email" name="email" required)

            label(for="password") Password:
            input(type="password" id="password" name="password" required)

            //- label(for="profilePicture") Profile Picture:
            //- input(type="text" id="profilePicture" name="profilePicture")

            //- label(for="coverPicture") Cover Picture:
            //- input(type="text" id="coverPicture" name="coverPicture")

            //- label(for="bio") Bio:
            //- textarea(id="bio" name="bio")

            //- label(for="location") Location:
            //- input(type="text" id="location" name="location")

            button(type="submit") Submit
            //- public/scripts/components/users/createNewUser.js
    script(src='scripts/components/users/createNewUser.js')