extends ../layout

block layout-content
    div.View
        div.Header
            h1.Banner AFRO LYRICS MANIA
            include ../components/buttons/NavButtons/topNavButtons
        div.NavButtons
            a(href="/admin/users")
                div.TopNavButton Users
        //- p Welcome: #{users.name} (#{users.email})
        //- textarea#virtual-text-area(readonly) 
        //- each user in users
                block layout-content

        div.Section
        div.Footer
            include ../components/buttons/NavButtons/bottomNavButtons

    //- script(src='scripts/components/users/getUserByID.js')
    //- views/layout.pug